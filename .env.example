# Root .env.example
# This is just for reference - actual configuration is in backend/.env and frontend/.env

# See backend/.env.example for backend configuration
# See frontend/.env.example for frontend configuration

# Quick Start:
# 1. cp backend/.env.example backend/.env
# 2. cp frontend/.env.example frontend/.env
# 3. Edit backend/.env with your credentials
# 4. Run: npm run dev (Docker) or npm run backend:dev & npm run frontend:dev (Local)
